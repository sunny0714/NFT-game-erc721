{
  "name": "nft-game-erc721",
  "version": "1.0.0",
  "scripts": {
    "compile": "truffle compile",
    "deploy": "truffle migrate --network rinkeby_infura --reset",
    "verify": "truffle run verify SunnyToken --network rinkeby_infura",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sunny0714",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.0.0",
    "dotenv": "^12.0.3",
    "web3": "^1.6.1"
  },
  "devDependencies": {
    "truffle-plugin-verify": "^0.5.20"
  }
}
